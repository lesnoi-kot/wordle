server {
  listen 80 default_server;

  root /usr/share/nginx/html;
  index index.html;

  location ~ ^${BASE_URL}(.*)$ {
    try_files /$1 /index.html =404;
  }

  location / {
    absolute_redirect off;
    return 301 ${BASE_URL};
  }
}
